- name: Clone infra ansible repo
  ansible.builtin.git:
    repo: https://github.com/pradeeksha1406/infra-ansible.git
    dest: /tmp/infra-ansible

- name: Run shell command
  shell: pwd
  register: out

- name: Debug
  debug:
    msg: "{{ out }}"