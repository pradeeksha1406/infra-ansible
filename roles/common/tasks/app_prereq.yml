- name: Cleanup old content
  ansible.builtin.file:
    path: "{{ app_loc }}"
    state: absent

- name: Create app directory
  ansible.builtin.file:
    path: "{{ app_loc }}"
    state: directory

#- name: Download backend Code and Extract it
#  ansible.builtin.unarchive:
#    src: https://expense-artifacts.s3.amazonaws.com/{{ component }}.zip
#    dest: "{{ app_loc }}"
#    remote_src: yes

- name: Download backend Code and Extract it
  ansible.builtin.unarchive:
    src: http://admin:Admin@123@artifactory.techadda.co/artifactory/{{ component }}/{{ component }}-{{ lookup('aws_ssm', '{{ component }}.app_version', region='us-east-1' ) }}.t5-arti.zip
    dest: "{{ app_loc }}"
    remote_src: yes