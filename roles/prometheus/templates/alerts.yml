groups:
  - name: Infra alerts
    rules:
      - alert: ServerDownAlert
        expr: up == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: Server Down Alert


      - alert: ServiceDownAlert
        expr: up ! == 1
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: Service Down Alert




